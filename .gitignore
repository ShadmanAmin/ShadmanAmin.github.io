# pixi environments
.pixi/*
!.pixi/config.toml

# Jekyll
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler
/vendor/
/.bundle/

# OS
.DS_Store
Thumbs.db

/jasper-master
/jasper-master/

# Original photos/video (~220 MB) used to generate assets/img and assets/video.
# Committed by default so the site can be rebuilt anywhere with `pixi run media`.
# If the repo gets unwieldy, uncomment the line below and back these up
# elsewhere — but note that nothing else in the repo can regenerate them.
#_media_src/
